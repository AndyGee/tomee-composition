The profiles contain configuration files that are specific to each environment.

The global configuration files are here: /src/main/tomee/conf/

All files are filtered on build and are seeded with properties defined in the pom.xml

Be careful.